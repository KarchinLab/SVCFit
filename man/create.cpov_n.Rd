% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree_score.R
\name{create.cpov_n}
\alias{create.cpov_n}
\title{Build the conditional probability-of-violation (CPOV) matrix}
\usage{
create.cpov_n(
  mcf_matrix,
  zero.thresh = 0.001,
  restriction.val = 1,
  tol = 1e-06
)
}
\arguments{
\item{mcf_matrix}{Numeric matrix or data.frame. CCF matrix (rows = clones,
columns = samples).}

\item{zero.thresh}{Numeric. CCF values below this threshold are treated as
zero.  Default \code{0.001}.}

\item{restriction.val}{Numeric. Penalty value for violating parent-child
pairs.  Default \code{1}.}

\item{tol}{Numeric. Tolerance added to parent CCF when checking violations.
Default \code{1e-6}.}
}
\value{
Integer matrix of dimension \eqn{(K+1) \times K} where rows are
labelled \code{"root"}, \code{"1"}, ..., \code{"K"} and columns are
\code{"1"}, ..., \code{"K"}.  Entries are \code{0} (no violation) or
\code{restriction_val} (violation).
}
\description{
Constructs a \eqn{(K+1) \times K} matrix encoding whether each directed
parent-child clone pair violates the CCF ordering constraint.  The root node
occupies row 1; clone \eqn{i} occupies row \eqn{i+1}.  Self-edges
(diagonal) are set to \code{restriction_val} to prevent self-loops.
}
