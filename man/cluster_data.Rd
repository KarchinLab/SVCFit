% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{cluster_data}
\alias{cluster_data}
\title{Perform full clustering pipeline on paired data}
\usage{
cluster_data(
  pair_path,
  pur_path,
  Kmax = 10,
  n_steps = 100,
  thr_min_w = 0.01,
  random_state = 0L,
  concentration = 1,
  min_n = 5,
  pair_num = 1
)
}
\arguments{
\item{pair_path}{Character. Path to file containing paired sample IDs.}

\item{pur_path}{Character. Path to file containing purity for each sample.}

\item{Kmax}{Numeric. Maximum k for DP-GMM clustering. Default is 10.}

\item{n_steps}{Numeric. Maximum steps for iteration. Default is 100.}

\item{thr_min_w}{Numeric. Minimum weight for active components. Default is 0.01.}

\item{random_state}{Integer. Random seed for reproducibility. Default is 0.}

\item{concentration}{Numeric. Dirichlet concentration prior. Default is 1.}

\item{min_n}{Integer. Minimum number of mutations per cluster. Default is 5.}

\item{pair_num}{Character or Numeric. The identifier for the specific sample pair being analyzed.}
}
\value{
A list containing two elements:
\enumerate{
\item \code{cluster_result}: Data frame with the final clustering centers and assignments.
\item \code{sv2cluster}: Data frame mapping specific SVs (CHROM, POS, END) to their assigned clusters.
}
}
\description{
This wrapper function executes the entire clustering workflow:
\enumerate{
\item Loads and processes data (\code{pre_process_cluster}).
\item Initializes the Python environment (\code{reticulate}).
\item Runs DP-GMM clustering on specified pairs.
\item Merges geometrically close clusters.
\item Enforces minimum cluster sizes.
\item Maps cluster assignments back to original Structural Variant (SV) data.
}
}
