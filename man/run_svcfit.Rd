% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_svcfit.R
\name{run_svcfit}
\alias{run_svcfit}
\title{Run the SVCFit SVCF inference pipeline}
\usage{
run_svcfit(
  p_het,
  p_onsv,
  p_sv,
  p_cnv,
  samp,
  exper,
  chr_lst = NULL,
  flank_del = 50,
  QUAL_thresh = 100,
  min_alt = 2,
  tum_only = FALSE,
  thresh = 0.1,
  flank_snp = 500,
  flank_cnv = 1000,
  hemizygous_chr = NULL,
  hemi_cn_bar = NULL,
  hemi_bg_cn = NULL,
  hemi_dup_r = 2,
  hemi_cn_override = NULL,
  zero_ref_allowlist = NULL
)
}
\arguments{
\item{p_het}{Character. Path to VCF file of heterozygous SNPs.}

\item{p_onsv}{Character. Path to VCF file of heterozygous SNPs on
SV-supporting reads.}

\item{p_sv}{Character. Path to VCF file of structural variants.}

\item{p_cnv}{Character. Path to CNV file (tab-delimited, FACETS format).}

\item{samp}{Character. Sample name written into the output table.}

\item{exper}{Character. Experiment identifier written into the output table.}

\item{chr_lst}{Character vector or \code{NULL}. Restrict analysis to these
chromosomes (e.g. \code{c("chr1","chr2")}). Default \code{NULL} (all
chromosomes).}

\item{flank_del}{Numeric. Maximum genomic distance (bp) for a deletion to be
considered overlapping a translocation. Default \code{50}.}

\item{QUAL_thresh}{Numeric. Minimum SV quality score. Default \code{100}.}

\item{min_alt}{Numeric. Minimum SV-supporting read count. Default \code{2}.}

\item{tum_only}{Logical. Whether SVs were called from a tumor-only BAM.
Default \code{FALSE}.}

\item{thresh}{Numeric (0, 1). Operational noise buffer around the sign-based
SV/CNV ordering criterion. The alternate candidate is used when
\code{ss1 <= thresh}; deletions always use it. Default \code{0.1}.}

\item{flank_snp}{Numeric. Flanking window (bp) used when mapping SNPs to SVs.
Default \code{500}.}

\item{flank_cnv}{Numeric. Flanking window (bp) used when assigning background
CNV segments. Default \code{1000}.}

\item{hemizygous_chr}{Character vector or \code{NULL}. Chromosomes single-copy in this
subject's germline (e.g. \code{c("chrX","chrY")}). \code{NULL} reproduces diploid-only behaviour.}

\item{hemi_cn_bar}{data.frame, numeric, or \code{NULL}. Read-depth mean copy number
(\code{cn_bar}) for hemizygous rows: a \code{data.frame(CHROM, POS, cn_bar)} or numeric vector.
\code{NULL} leaves copy-altered hemizygous rows unresolved rather than guessing.}

\item{hemi_bg_cn}{data.frame, numeric, or \code{NULL}. Flanking (background) copy number for the
CNV-first deletion form; optional.}

\item{hemi_dup_r}{Numeric. Copies in carrier cells for a hemizygous tandem duplication.
Default \code{2}; duplication SVCFs are reported as upper bounds.}

\item{hemi_cn_override}{data.frame or \code{NULL}. Externally-derived hemizygous copy number
replacing the default single-copy germline state.}

\item{zero_ref_allowlist}{data.frame or \code{NULL}. Hemizygous rows with \code{sv_ref = 0} that
BAM evidence confirms are genuine clonal losses (SVCF = VAF = 1).}
}
\value{
A named list with two elements:
\describe{
\item{\code{svcf}}{data.frame. Full SVCF result table from
\code{\link{calc_svcf}}.}
\item{\code{sv_info}}{data.frame. Parsed SV metadata from
\code{extract_info}.}
}
}
\description{
Runs the three-stage SVCF inference pipeline: VCF/CNV parsing
(\code{extract_info}), SV characterisation (\code{characterize_sv}), and
structural variant cellular fraction calculation (\code{calc_svcf}).  Clustering and tree
building are handled separately by \code{\link{build_trees}}.
}
\examples{
\dontrun{
res <- run_svcfit(
  p_het  = "het_snp.vcf",
  p_onsv = "het_on_sv.vcf",
  p_sv   = "sv.vcf",
  p_cnv  = "cnv.txt",
  samp   = "SAMPLE_01",
  exper  = "EXP_01"
)
head(res$svcf)
}

}
\seealso{
\code{\link{build_trees}} for downstream clustering and tree
building using the BED files produced from \code{svcf}.
}
