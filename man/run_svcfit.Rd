% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_svcfit.R
\name{run_svcfit}
\alias{run_svcfit}
\title{Run the complete SVCFit pipeline}
\usage{
run_svcfit(
  p_het,
  p_onsv,
  p_sv,
  p_cnv,
  samp,
  exper,
  chr_lst = NULL,
  flank_del = 50,
  QUAL_tresh = 100,
  min_alt = 2,
  tum_only = FALSE,
  thresh = 0.1,
  flank_snp = 500,
  flank_cnv = 1000,
  run_clustering = FALSE,
  pair_path = NULL,
  pur_path = NULL,
  data_dir = NULL,
  pair_num = 1L,
  exclude_pairs = integer(0),
  Kmax = 10L,
  n_steps = 100L,
  thr_min_w = 0.01,
  random_state = 0L,
  concentration = 1,
  min_n = 5L,
  run_tree = FALSE,
  lineage_precedence_thresh = 0.2,
  sum_filter_thresh = 0.2
)
}
\arguments{
\item{p_het}{Character. Path to VCF file of heterozygous SNPs.}

\item{p_onsv}{Character. Path to VCF file of heterozygous SNPs on
SV-supporting reads.}

\item{p_sv}{Character. Path to VCF file of structural variants.}

\item{p_cnv}{Character. Path to CNV file (tab-delimited, FACETS format).}

\item{samp}{Character. Sample name written into the output table.}

\item{exper}{Character. Experiment identifier written into the output table.}

\item{chr_lst}{Character vector or NULL. Restrict analysis to these
chromosomes (e.g. \code{c("chr1","chr2")}). Default \code{NULL} (all
chromosomes).}

\item{flank_del}{Numeric. Maximum genomic distance (bp) for a deletion to be
considered overlapping a translocation. Default \code{50}.}

\item{QUAL_tresh}{Numeric. Minimum SV quality score. Default \code{100}.}

\item{min_alt}{Numeric. Minimum SV-supporting read count. Default \code{2}.}

\item{tum_only}{Logical. Whether SVs were called from a tumor-only BAM.
Default \code{FALSE}.}

\item{thresh}{Numeric (0, 1). Threshold for deciding SV/CNV event ordering
in SVCF calculation. Default \code{0.1}.}

\item{flank_snp}{Numeric. Flanking window (bp) used when mapping SNPs to SVs.
Default \code{500}.}

\item{flank_cnv}{Numeric. Flanking window (bp) used when assigning background
CNV segments. Default \code{1000}.}

\item{run_clustering}{Logical. Run DP-GMM clustering on paired samples.
Default \code{FALSE}.}

\item{pair_path}{Character. Path to a tab-delimited file with paired sample
IDs (columns: \code{pre_BAT}, \code{on_BAT}). Required when
\code{run_clustering = TRUE}.}

\item{pur_path}{Character. Path to a tab-delimited purity file (must contain
columns \code{sample} and \code{purity}). Required when
\code{run_clustering = TRUE}.}

\item{data_dir}{Character. Root directory containing SVCFit output BED files.
Expected layout: \code{<data_dir>/COMBAT/SVCFit_output/<sample_ID>.bed}.
Required when \code{run_clustering = TRUE}.}

\item{pair_num}{Integer. Which pair index to extract final clone CCFs for
(used as input to tree building). Default \code{1L}.}

\item{exclude_pairs}{Integer vector. Pair indices to exclude from clustering.
Default \code{integer(0)} (no pairs excluded).}

\item{Kmax}{Integer. Maximum number of DP-GMM components. Default \code{10L}.}

\item{n_steps}{Integer. Number of warm-start EM steps for convergence
diagnostics. Default \code{100L}.}

\item{thr_min_w}{Numeric. Minimum component weight threshold below which a
component is considered inactive. Default \code{0.01}.}

\item{random_state}{Integer. Python random seed for reproducibility.
Default \code{0L}.}

\item{concentration}{Numeric. Dirichlet process concentration parameter.
Default \code{1}.}

\item{min_n}{Integer. Minimum number of events per cluster; smaller clusters
are merged into the nearest larger one. Default \code{5L}.}

\item{run_tree}{Logical. Build a phylogenetic tree from cluster CCFs.
Requires \code{run_clustering = TRUE}. Default \code{FALSE}.}

\item{lineage_precedence_thresh}{Numeric. Maximum allowed CCF ratio for a
child-to-parent edge to be kept in the tree graph. Default \code{0.2}.}

\item{sum_filter_thresh}{Numeric. Maximum allowed excess of children CCF
sum over parent CCF. Default \code{0.2}.}
}
\value{
A named list with elements:
\describe{
\item{\code{svcf}}{data.frame. Full SVCF result table from
\code{\link{calc_svcf}}.}
\item{\code{cluster}}{(only when \code{run_clustering = TRUE}) List
returned by \code{\link{cluster_data}}: \code{[[1]]} cluster assignments,
\code{[[2]]} SV-to-cluster mapping, \code{[[3]]} clone CCF table.}
\item{\code{tree}}{(only when \code{run_tree = TRUE}) List returned by
\code{\link{build_tree}}: \code{[[1]]} best spanning tree edges,
\code{[[2]]} CCF matrix, \code{[[3]]} igraph tree plot.}
}
}
\description{
Master function that runs SVCF inference and, optionally, DP-GMM clustering
and phylogenetic tree building.
}
\section{Pipeline stages}{

\enumerate{
\item \strong{SVCF inference} (always runs): \code{extract_info} →
\code{characterize_sv} → \code{calc_svcf}.
\item \strong{Clustering} (when \code{run_clustering = TRUE}): runs
\code{cluster_data} on paired pre/on-treatment samples using a
Dirichlet-process Gaussian mixture model (requires Python sklearn
via reticulate).
\item \strong{Tree building} (when \code{run_tree = TRUE}): infers the
best-scoring phylogenetic spanning tree from cluster CCFs via
\code{build_tree}. Requires \code{run_clustering = TRUE}.
}
}

\examples{
\dontrun{
# SVCF inference only
res <- run_svcfit(
  p_het  = "het_snp.vcf",
  p_onsv = "het_on_sv.vcf",
  p_sv   = "sv.vcf",
  p_cnv  = "cnv.txt",
  samp   = "SAMPLE_01",
  exper  = "EXP_01"
)
head(res$svcf)

# SVCF inference + clustering + tree building
res <- run_svcfit(
  p_het          = "het_snp.vcf",
  p_onsv         = "het_on_sv.vcf",
  p_sv           = "sv.vcf",
  p_cnv          = "cnv.txt",
  samp           = "SAMPLE_01",
  exper          = "EXP_01",
  run_clustering = TRUE,
  pair_path      = "pairs.txt",
  pur_path       = "purity.txt",
  pair_num       = 1L,
  run_tree       = TRUE
)
res$tree[[3]]  # igraph plot of best tree
}

}
