% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_svcfit.R
\name{build_trees}
\alias{build_trees}
\title{Cluster SVs and build a phylogenetic tree from SVCF output}
\usage{
build_trees(
  run_clustering = FALSE,
  pair_path = NULL,
  pur_path = NULL,
  data_dir = NULL,
  pair_num = 1L,
  exclude_pairs = integer(0),
  Kmax = 10L,
  n_steps = 100L,
  thr_min_w = 0.01,
  random_state = 0L,
  concentration = 1,
  min_n = 5L,
  min_dist = 0.2,
  deduplicate = TRUE,
  run_tree = FALSE,
  lineage_precedence_thresh = 0.2,
  sum_filter_thresh = 0.2,
  ccf_floor = 0.1,
  linear_penalty = 0
)
}
\arguments{
\item{run_clustering}{Logical. Run DP-GMM clustering on paired samples.
Default \code{FALSE}.}

\item{pair_path}{Character or \code{NULL}. Path to a tab-delimited file with
paired sample IDs (columns: \code{pre_BAT}, \code{on_BAT}). Required when
\code{run_clustering = TRUE}.}

\item{pur_path}{Character or \code{NULL}. Path to a tab-delimited purity file
(must contain columns \code{sample} and \code{purity}). Required when
\code{run_clustering = TRUE}.}

\item{data_dir}{Character or \code{NULL}. Root directory containing SVCFit
output BED files. Expected layout:
\code{<data_dir>/COMBAT/SVCFit_output/<sample_ID>.bed}. Required when
\code{run_clustering = TRUE}.}

\item{pair_num}{Integer. Pair index whose clone CCF table is passed to tree
building. Default \code{1L}.}

\item{exclude_pairs}{Integer vector. Pair indices to exclude from clustering.
Default \code{integer(0)} (no pairs excluded).}

\item{Kmax}{Integer. Maximum number of DP-GMM components. Default \code{10L}.}

\item{n_steps}{Integer. Number of warm-start EM steps. Default \code{100L}.}

\item{thr_min_w}{Numeric. Minimum component weight; components below this
threshold are considered inactive. Default \code{0.01}.}

\item{random_state}{Integer. Python random seed for reproducibility.
Default \code{0L}.}

\item{concentration}{Numeric. Dirichlet process concentration parameter.
Default \code{1}.}

\item{min_n}{Integer. Minimum number of events per cluster; smaller clusters
are merged into the nearest larger one. Default \code{5L}.}

\item{min_dist}{Numeric. Distance threshold for merging geometrically close
cluster centres after DP-GMM fitting. Default \code{0.2}.}

\item{deduplicate}{Logical. Remove duplicate (pre_BAT, on_BAT) rows before
fitting the DP-GMM. Default \code{TRUE}.}

\item{run_tree}{Logical. Build a phylogenetic tree from cluster CCFs.
Requires \code{run_clustering = TRUE}. Default \code{FALSE}.}

\item{lineage_precedence_thresh}{Numeric. Maximum allowed CCF ratio for a
child-to-parent edge to be retained. Default \code{0.2}.}

\item{sum_filter_thresh}{Numeric. Maximum allowed excess of summed children
CCFs over the parent CCF. Default \code{0.2}.}

\item{ccf_floor}{Numeric. CCF values below this threshold are zeroed out
before clustering. Default \code{0.1}. Lower at low purity (e.g. 0.05 at
p40) to retain signal from small subclones.}

\item{linear_penalty}{Numeric. Per-edge penalty added to the tree fitness
score to favour simpler topologies. Default \code{0} (no penalty).}
}
\value{
A named list with two elements (\code{NULL} when the corresponding
stage did not run):
\describe{
\item{\code{cluster}}{List returned by \code{\link{cluster_data}}:
\code{[[1]]} cluster assignments, \code{[[2]]} SV-to-cluster mapping,
\code{[[3]]} clone CCF table for \code{pair_num}.
\code{NULL} when \code{run_clustering = FALSE}.}
\item{\code{tree}}{List returned by \code{\link{build_tree}} (only when
\code{run_tree = TRUE} and \eqn{\ge 2} clones exist):
\code{[[1]]} best spanning tree edges, \code{[[2]]} CCF matrix,
\code{[[3]]} igraph tree plot.
\code{NULL} when \code{run_tree = FALSE} or fewer than 2 clones.}
}
}
\description{
Takes SVCFit BED files produced by \code{\link{run_svcfit}} and optionally
runs DP-GMM clustering on paired pre/on-treatment samples, then infers the
best-scoring phylogenetic spanning tree from the resulting clone CCFs.
}
\section{Pipeline stages}{

\enumerate{
\item \strong{Clustering} (when \code{run_clustering = TRUE}): fits a
Dirichlet-process Gaussian mixture model via \code{cluster_data}
(requires Python \pkg{sklearn} via \pkg{reticulate}).
\item \strong{Tree building} (when \code{run_tree = TRUE}): infers the
best-scoring phylogenetic spanning tree from clone CCFs via
\code{build_tree}. Requires \code{run_clustering = TRUE}.
}
}

\examples{
\dontrun{
# Clustering only
bt <- build_trees(
  run_clustering = TRUE,
  pair_path      = "pairs.txt",
  pur_path       = "purity.txt",
  data_dir       = "/path/to/svcfit_output"
)

# Clustering + tree
bt <- build_trees(
  run_clustering = TRUE,
  pair_path      = "pairs.txt",
  pur_path       = "purity.txt",
  data_dir       = "/path/to/svcfit_output",
  pair_num       = 1L,
  run_tree       = TRUE
)
bt$tree[[3]]  # igraph plot of best tree
}

}
